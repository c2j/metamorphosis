name: QED Verification

on:
  push:
    branches: [main, develop]
    paths:
      - "crates/qed/**"
      - "crates/core/**"
      - "crates/rules/**"
      - "Cargo.toml"
      - "Cargo.lock"
  pull_request:
    branches: [main]
    paths:
      - "crates/qed/**"
      - "crates/core/**"
      - "crates/rules/**"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Install QED prover
        run: chmod +x scripts/install-qed-prover.sh && ./scripts/install-qed-prover.sh
        continue-on-error: true

      - name: Build
        run: cargo build --workspace

      - name: Run all tests
        run: cargo test --workspace

      - name: Run QED-specific tests (with prover)
        run: cargo test -p metamorphosis-qed -- --include-ignored
        continue-on-error: true

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - run: cargo fmt --all -- --check
      - run: cargo clippy --workspace -- -D warnings
        continue-on-error: true
